{{#> body}}

<div class="panel">
	<div class="panel_body">
		<div class="row">
			<div class="col-md-8 offset-md-2">
				user's name: {{user_name}}
				<h1>{{name}}</h1>
				<h2>{{specialty}}</h2>
				<p>{{background}}</p>
			</div>
		</div>
	</div>
</div>

{{/body}}